{
  "name": "qna-portal-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node dev-runner.js start",
    "lint": "node dev-runner.js lint",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "node dev-runner.js start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.39.2"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
